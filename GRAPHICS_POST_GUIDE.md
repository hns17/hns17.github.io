# Graphics Post Guide

이 문서는 `_posts/Graphics/Series`에 작성하는 엔진 독립적 그래픽스 연재의 전용 지침이다.
일반적인 글 구조와 편집 원칙은 `POST_STYLE_GUIDE.md`를 따르고, 그래픽스 연재의 주제 순서,
이미지, 역사 서술, 예약 작성 규칙은 이 문서를 따른다.

## 연재 목표

- 그래픽스 용어를 사전처럼 나열하지 않고 `문제 → 기존 해결 → 한계 → 다음 기술`의 인과로 설명한다.
- 빛, 시각, 수학, 렌더링 알고리즘과 하드웨어가 서로 어떻게 연결되는지 보여 준다.
- 특정 엔진, 에디터 메뉴나 버전에 종속되지 않는 원리를 다룬다.
- 각 글은 독립적으로 읽을 수 있어야 하지만, 전체 연재에서는 앞 글의 결론이 다음 글의 질문이 되어야 한다.
- 연재 순서는 `GRAPHICS_SERIES_PLAN.md`를 유일한 기준으로 삼는다.

## 범위

포함하는 주제:

- 빛과 색, 시각과 카메라
- 벡터, 좌표 공간, 투영과 기하
- 래스터라이제이션, 샘플링과 GPU 파이프라인
- 셰이딩, 텍스처, 재질과 PBR
- 그림자, 전역 조명, Ray Tracing과 Path Tracing
- Forward, Deferred, Forward+, 투명도와 볼륨 렌더링
- 후처리, 이미지 품질과 일반적인 렌더링 성능

제외하는 주제:

- Unity, Unreal 등 특정 엔진의 설정과 구현
- 특정 엔진의 렌더 파이프라인, 컴포넌트, 디버거와 프로파일러 사용법
- 특정 엔진 버전에 따른 기능 지원 상태

엔진별 글은 이 연재를 선행 지식으로 사용하는 별도 연재에서 다룬다.

## 순서 원칙

1. `GRAPHICS_SERIES_PLAN.md`에서 가장 앞에 있는 미완료 항목 하나를 선택한다.
2. 앞 항목이 미완료라면 뒤 항목을 먼저 작성하지 않는다.
3. 한 번의 수동 작업 또는 예약 실행에서는 글 하나만 작성한다.
4. 수동 작업에서 같은 `series_id`의 초안이 있다면 새 글을 만들지 않고 사용자 요청에 따라 수정한다.
5. 예약 실행에서 같은 `series_id`의 초안이 발견되면 수정하거나 다음 글로 넘어가지 않고 중단한다.
6. Markdown, 이미지와 Jekyll 빌드 검증은 검토 가능한 초안의 조건이며 연재 완료 조건이 아니다.
7. 사용자의 명시적 승인, 완료 표시를 포함한 커밋과 원격 푸시가 모두 성공해야 계획표 항목이 완료된다.
8. 실패하거나 자료가 부족하면 항목을 완료하지 않는다. 다음 글도 시작하지 않는다.
9. 주제를 더 나눌 필요가 있으면 현재 항목 바로 뒤에 하위 항목을 추가하고, 이후 ID의 순서는 유지한다.

## 상태 전이

연재 글은 다음 순서로만 진행한다.

1. **미작성**: 계획표는 `[ ]`이고 같은 `series_id`의 문서가 없다.
2. **작성·검토 중**: 계획표는 `[ ]`이고 초안이 존재한다. 사용자가 필요할 만큼 수정을 반복한다.
3. **승인 대기**: 검증을 통과해도 계획표는 `[ ]`로 유지한다. Codex가 임의로 완료를 판단하지 않는다.
4. **완료**: 사용자가 명시적으로 승인한 뒤 완료 표시를 커밋하고 푸시하여 원격 반영까지 확인했다.

작성·검토 중이거나 승인 대기인 글이 하나라도 있으면 예약 실행은 파일을 변경하지 않는다. 다음 글은
직전 글이 완료 상태가 된 뒤의 예약 실행에서만 시작할 수 있다.

## 모든 글이 답해야 하는 질문

각 글은 다음 질문에 자연스럽게 답해야 한다.

1. 그래픽스에서 왜 이 이야기를 하는가?
2. 이 기술이 없었을 때 어떤 문제가 있었는가?
3. 당시의 연구자나 개발자는 어떤 제약을 받고 있었는가?
4. 제안된 원리나 알고리즘은 문제를 어떻게 해결하는가?
5. 그 해결책에는 어떤 한계나 비용이 남았는가?
6. 이 한계가 다음 글의 기술을 왜 필요하게 만드는가?

역사적 사건은 흥미를 위한 장식이 아니라 기술의 필요성을 설명하는 근거로 사용한다.

## 권장 문서 구조

### 핵심 요약

글 시작 3~5줄 안에 다음 내용을 요약한다.

- 이번 글이 답하는 질문
- 핵심 원리
- 다음 개념과의 연결

### 출발 질문

독자가 관찰할 수 있는 현상이나 이전 글에서 남은 문제로 시작한다.

예:

> 3D 모델이 존재한다는 사실만으로 그 모델이 보이는 것은 아니다.
> 사람과 카메라가 보는 것은 물체에서 도달한 빛이다.
> 그렇다면 컴퓨터는 어떤 빛이 한 픽셀에 도달했는지 어떻게 계산할까?

### 왜 이 문제가 생겼는가

- 앞 글에서 무엇까지 해결했는지 짧게 연결한다.
- 현재 기술이 없으면 화면에서 어떤 문제가 보이는지 설명한다.
- 수식이나 구현보다 관찰 가능한 현상을 먼저 제시한다.

### 역사적 배경

- 인물, 논문, 제품이나 하드웨어는 기술적 전환에 직접 관련될 때만 넣는다.
- `누가 만들었다`에서 끝내지 않고 `당시 무엇이 불가능하거나 비쌌는가`를 설명한다.
- 날짜, 논문명과 기술적 기여는 원 논문, 표준 문서, 기관 기록 등 1차 출처를 우선해 검증한다.
- 서로 다른 자료가 기여 범위를 다르게 설명하면 단정하지 말고 차이를 밝힌다.

### 직관적인 설명

- 수식 전에 그림, 비유나 단순한 장면으로 관계를 설명한다.
- 비유는 실제 모델과 달라지는 지점을 함께 적는다.
- 한 절에서 새 핵심 용어를 너무 많이 도입하지 않는다.

### 수학과 알고리즘

- 기호를 사용하기 전에 각 기호가 무엇을 뜻하는지 정의한다.
- 수식은 결과만 제시하지 않고 각 항이 어떤 물리적·기하학적 역할을 하는지 설명한다.
- 의사 코드나 코드 블록 뒤에는 무엇을 확인해야 하는지 1~3줄로 설명한다.
- 정규화, 좌표 공간, 단위와 가정이 결과에 미치는 영향을 생략하지 않는다.

### 해결한 것과 남은 것

- 기술이 해결한 문제를 명확히 정리한다.
- 품질, 성능, 메모리, 정확성 측면의 한계를 구분한다.
- 오래된 기법을 단순히 틀렸거나 폐기된 기술로 표현하지 않는다.
- 당시 환경에서는 왜 합리적인 선택이었는지 설명한다.

### 다음 글로 연결

마지막 정리에서 다음 글의 질문을 한두 문장으로 예고한다.

예:

> Lambert 모델로 표면의 기본 밝기는 계산할 수 있게 되었다.
> 하지만 금속과 플라스틱이 서로 다르게 반짝이는 현상은 아직 설명하지 못한다.
> 다음 글에서는 표면 위의 하이라이트가 어디에서 오는지 살펴본다.

## 이미지 원칙

### 기본 기준

- 모든 그래픽스 연재 글에는 최소 한 개의 핵심 설명 이미지가 있어야 한다.
- 보통 길이의 글은 2~4개의 시각 자료를 권장한다.
- 이미지 수를 채우기 위한 장식 이미지는 넣지 않는다.
- 각 이미지는 독자의 질문 하나를 해결해야 한다.
- 관계, 공간, 변화, 비교 또는 알고리즘 흐름을 글보다 빠르게 이해하게 할 때만 사용한다.

### 우선 사용할 이미지

1. 직접 제작한 SVG 구조도와 벡터 도해
2. 같은 조건에서 한 변수만 바꾼 렌더링 비교
3. 좌표, 벡터, 광선과 표면 관계를 보여 주는 기하 도해
4. 파이프라인과 데이터 흐름을 보여 주는 단계도
5. 그래프, 색 공간, 스펙트럼과 측정 단위 시각화
6. 시간에 따른 변화가 핵심일 때 사용하는 짧은 애니메이션
7. 역사 설명에 꼭 필요한 원자료 이미지 또는 직접 다시 그린 재구성도

간단한 목록이나 두 값의 비교는 이미지로 만들지 말고 표나 문장으로 설명한다.

### 이미지 계획

본문을 쓰기 전에 다음을 먼저 정한다.

- 이 글에서 독자가 가장 이해하기 어려운 관계는 무엇인가?
- 전후 비교가 필요한가?
- 공간 방향이나 좌표를 보여 줘야 하는가?
- 역사 자료가 기술 등장 배경을 실제로 설명하는가?
- 정지 이미지로 충분한가, 시간 변화가 필요한가?

각 이미지에는 `목적`, `형식`, `독자가 확인할 것`이 있어야 한다.

예:

| 목적 | 적절한 이미지 |
| --- | --- |
| `N · L`의 의미 설명 | 법선과 빛 방향의 각도가 다른 세 표면 도해 |
| Gouraud와 Phong 비교 | 동일한 저해상도 구체의 나란한 렌더링 |
| Forward와 Deferred 비교 | 각 경로의 Pass와 버퍼 흐름도 |
| Mipmap 설명 | 거리별 화면 크기와 선택되는 Mip Level 도해 |
| Shadow Bias 설명 | Acne, 적정 Bias, Peter Panning 비교 |

### 정확성

- 수학적 관계, 벡터 방향, 좌표축, 단위와 레이블은 본문과 일치해야 한다.
- 정확한 도식은 SVG, 그래프 생성 코드나 검증 가능한 렌더 결과로 만든다.
- 생성형 이미지에는 정확한 수식, 축, 벡터 관계나 비교 결과를 맡기지 않는다.
- 생성형 이미지는 개념적 표지나 분위기 설명에 도움이 될 때만 제한적으로 사용한다.
- 색만으로 의미를 구분하지 않고 선 모양, 패턴, 레이블을 함께 사용한다.
- 축소된 모바일 화면에서도 핵심 레이블을 읽을 수 있어야 한다.

### 출처와 저작권

- 새 글은 외부 서버 이미지에 직접 의존하지 않고 저장소의 로컬 자산을 사용한다.
- 직접 만든 이미지는 캡션에 `직접 제작`이라고 적는다.
- 외부 자료는 라이선스가 허용할 때만 저장하며, 캡션과 참고 절에 원문 링크와 라이선스를 남긴다.
- 논문 그림이나 상용 제품 이미지를 허가 없이 복사하지 않는다.
- 원자료가 필요하지만 재사용 조건이 불명확하면 관계만 직접 다시 그린다.
- 외부 이미지를 참고해 다시 그린 경우 원자료 링크와 `원리를 바탕으로 재구성`이라고 적는다.

### 경로와 이름

글별 이미지 폴더:

```text
assets/images/posts/graphics-series/g001-why-graphics-starts-with-light/
```

파일 이름:

```text
scene-light-eye-flow.svg
shading-shadow-comparison.png
```

규칙:

- 폴더에는 소문자 영문, 숫자와 하이픈만 사용한다.
- `image1.png`, `final-final.png` 같은 의미 없는 이름을 사용하지 않는다.
- 새 글의 이미지는 해당 글 전용 폴더에 보관한다.
- `_posts/Graphics/Series`의 글에서는 검증된 상대 경로를 사용한다.

예:

```markdown
![물체에서 반사된 빛이 카메라에 도달해 픽셀이 되는 흐름](../../../assets/images/posts/graphics-series/g001-why-graphics-starts-with-light/scene-light-eye-flow.svg)

*그림 1. 3D 그래픽스가 물체 자체가 아니라 카메라에 도달하는 빛을 계산한다는 관계. 직접 제작.*
```

### 이미지 앞뒤 설명

- 이미지 앞에는 왜 이 그림을 봐야 하는지 설명한다.
- 이미지 뒤에는 독자가 확인해야 할 차이나 결론을 설명한다.
- 대체 텍스트는 `그림`, `image`가 아니라 실제 내용을 기술한다.
- 캡션에는 그림 번호, 핵심 의미와 출처를 적는다.
- 본문에서 이미지를 `아래 그림`으로만 부르지 말고 핵심 관계를 문장으로도 설명한다.

## 본문 링크와 예시 원칙

독자가 본문을 읽다가 고유한 실험, 착시, 데모, 논문이나 역사적 사례를 만났을 때 참고 절까지
내려가지 않고 그 자리에서 확인할 수 있어야 한다.

- 고유한 실험, 착시, 데모와 논문은 처음 언급하는 문장에 원자료 링크를 건다.
- 참고 절의 링크만으로 본문의 첫 등장 링크를 대신하지 않는다.
- 글의 핵심 주장을 시각적으로 증명하는 정지 예시는 사용 허가를 확인해 로컬 이미지로 포함하거나,
  공식 원본과 비교·증명 페이지를 바로 연결한다.
- 움직임이나 상호작용이 핵심인 사례는 정지 화면만 넣지 않고 연구기관 또는 저자의 공식 데모를 연결한다.
- 원 논문은 논문의 제목이나 기여를 설명하는 문장에서 바로 연결한다.
- 링크를 열기 전에 무엇을 확인해야 하는지 한두 문장으로 안내한다.
- 설명이 현재 글의 흐름을 크게 벗어나면 연재 순서를 지켜 별도 글로 작성한다. 아직 작성하지 않은
  문서의 빈 링크나 내용 없는 자리표시자 문서는 만들지 않는다.
- 관련 글이 승인·게시되면 기존 글의 첫 용어 등장 위치에 내부 링크를 추가한다.
- 외부 링크는 작성 시점에 접근 가능한지 확인하고, 접근이 어려운 자료는 DOI나 기관의 영구 링크를
  함께 사용한다.

## front matter

새 연재 글은 다음 필드를 사용한다.

```yaml
---
title: "[빛에서 픽셀까지 001] 왜 3D 그래픽스는 빛에서 시작하는가"
categories: [Graphics/Series/Light-To-Pixel]
tags: ["Graphics", "Light", "Rendering"]
series: graphics-foundations
series_title: "빛에서 픽셀까지"
series_url: "/category/graphics/series/light-to-pixel/"
series_id: G001
series_order: 1
---
```

규칙:

- `Graphics/Series`는 여러 그래픽스 연재를 담는 개념적 상위 경로일 뿐, 글의 실제 카테고리로 사용하지 않는다.
- 각 연재는 `Graphics/Series/<Series-Key>` 형식의 고유한 카테고리를 사용한다.
- 각 연재는 `_data/graphics_series.yml`에 고유한 내부 ID, 표시 제목, 카테고리, 소개와 전용 페이지를 등록한다.
- 각 연재는 다른 연재와 분리된 계획표를 사용한다.
- 제목은 `[연재 제목 NNN] 주제` 형식을 사용한다.
- `NNN`은 `series_order`를 세 자리 0 채움으로 표시한다. 예: `1 → 001`, `12 → 012`, `100 → 100`.
- `series`, `series_title`과 `series_url`은 시리즈 등록 정보와 일치해야 한다.
- 표시 번호와 `series_id`의 숫자는 일치해야 하며, 승인·게시된 번호는 이후 임의로 바꾸지 않는다.
- 전체 시리즈 모음은 연재별로 글을 묶고, 각 연재 페이지는 `series_order` 오름차순으로 표시한다.
- `series_id`와 `series_order`는 `GRAPHICS_SERIES_PLAN.md`와 일치해야 한다.
- 실제 작성일을 파일 이름의 날짜로 사용한다.
- 같은 `series_id`를 가진 글을 둘 이상 만들지 않는다.
- 본문에 front matter의 제목을 `#`으로 반복하지 않는다.

## 예약 작성 흐름

예약 작업은 매 실행마다 다음 순서로 진행한다.

1. `AGENTS.md`를 읽는다.
2. `POST_STYLE_GUIDE.md`를 읽는다.
3. `GRAPHICS_POST_GUIDE.md`를 읽는다.
4. `GRAPHICS_SERIES_PLAN.md`에서 가장 앞의 미완료 항목을 찾는다.
5. 같은 `series_id`의 기존 글과 이미지 폴더가 있는지 확인한다.
6. 기존 초안이나 이미지 폴더가 있으면 사용자 검토 중인 글로 판단하고 파일을 수정하지 않은 채 종료한다.
7. 기존 초안이 없을 때만 공식 표준, 원 논문, 기관 기록과 교재급 1차 자료를 우선 조사한다.
8. 출발 질문과 시각 자료 계획을 먼저 만든다.
9. 본문과 최소 한 개의 핵심 이미지를 작성한다.
10. 용어, 수식, 날짜, 인물의 기여와 이미지 출처를 다시 검증한다.
11. Markdown 원본에서 이미지 경로를 확인한다.
12. Jekyll 빌드를 실행한다.
13. 생성된 HTML에서 제목, 이미지, 캡션과 참고 링크를 확인한다.
14. 검증이 성공해도 계획표는 `[ ]`로 유지하고 사용자에게 검토를 요청한다.
15. 한 실행에서 다음 글까지 이어서 작성하지 않는다.

예약 작업은 초안 작성과 검증까지만 수행하며 commit, push, 완료 표시 또는 외부 게시를 수행하지 않는다.
사용자가 검토 과정에서 수정을 요청하면 수동 작업으로 같은 초안을 고친다.

사용자가 현재 글의 완료를 명시적으로 승인한 수동 작업에서는 다음 순서로 처리한다.

1. 마지막 수정 사항과 Jekyll 빌드를 다시 검증한다.
2. 현재 항목만 `[x]`로 바꾼다.
3. 승인된 글, 이미지와 완료 표시를 하나의 의도적인 변경으로 커밋한다.
4. 현재 브랜치를 원격 저장소에 푸시한다.
5. 완료 커밋이 원격 브랜치에 반영되었는지 확인한다.
6. 모든 단계가 성공한 뒤에만 사용자에게 연재 완료를 알린다.

커밋이나 푸시가 실패하면 완료로 보고하지 않으며 예약 연재도 계속 중단한다.

## 완료 조건

아래 조건이 모두 충족되어야 계획표를 완료 처리하고 다음 글의 예약 작성을 허용할 수 있다.

- [ ] 올바른 `series_id`와 순서가 사용되었다.
- [ ] 카테고리가 시리즈 등록 정보의 고유 카테고리와 일치하고 제목에 연재 제목과 일치하는 세 자리 번호가 있다.
- [ ] `series`, `series_title`, `series_url`이 시리즈 등록 정보와 일치한다.
- [ ] 앞선 미완료 글을 건너뛰지 않았다.
- [ ] 첫 부분에서 그래픽스에서 이 주제가 필요한 이유를 설명했다.
- [ ] 역사적 배경이 문제와 기술 변화에 연결된다.
- [ ] 직관적 설명 뒤에 필요한 수학과 알고리즘이 나온다.
- [ ] 해결한 문제와 남은 한계가 구분된다.
- [ ] 다음 글로 이어지는 질문이 있다.
- [ ] 핵심 설명 이미지가 최소 한 개 있다.
- [ ] 모든 이미지에 의미 있는 대체 텍스트와 캡션이 있다.
- [ ] 외부 자료의 출처와 라이선스를 확인했다.
- [ ] 이미지가 모바일 폭에서도 읽힌다.
- [ ] Markdown과 Jekyll 결과에서 이미지 경로가 유효하다.
- [ ] 공식·1차 출처를 우선한 참고 절이 있다.
- [ ] 고유한 실험, 착시, 데모와 논문이 첫 등장 위치에서 원자료 또는 예시로 연결된다.
- [ ] 움직임이 핵심인 사례에는 실제 움직임을 확인할 수 있는 공식 데모가 있다.
- [ ] Jekyll 빌드가 성공했다.
- [ ] 사용자가 현재 결과를 명시적으로 최종 승인했다.
- [ ] 완료 표시를 포함한 변경이 커밋되었다.
- [ ] 완료 커밋이 원격 저장소에 푸시된 것을 확인했다.

## 중단 조건

다음 상황에서는 임의로 진행하지 않고 현재 항목을 미완료로 남긴다.

- 앞선 순서의 글이 미완료인데 뒤 글을 선택한 경우
- 핵심 기술 주장이나 역사적 기여를 신뢰할 수 있는 출처로 확인할 수 없는 경우
- 필요한 이미지를 정확하게 만들거나 합법적으로 사용할 수 없는 경우
- 기존 사용자 변경과 안전하게 분리할 수 없는 경우
- Jekyll 빌드 또는 이미지 경로 검증이 실패한 경우
- 같은 `series_id`의 초안이 있어 사용자 검토·수정 또는 완료 승인을 기다리는 경우
- 사용자의 명시적 완료 승인이 없는 경우
- 완료 커밋 또는 원격 푸시가 실패한 경우
