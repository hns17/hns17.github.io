# Post Style Guide

이 문서는 기존 포스트를 읽기 쉽게 재구성할 때 사용하는 기준이다.

## 공통 원칙

1. 본문에 `#` 제목을 다시 쓰지 않는다.
2. 글 시작 3~5줄 안에 `핵심 요약`을 둔다.
3. 본문 큰 구간은 `##`, 하위 설명은 `###`까지만 사용한다.
4. `------` 같은 구분선은 꼭 필요한 경우가 아니면 쓰지 않는다.
5. 코드 블록 뒤에는 반드시 `무엇을 보여 주는 코드인지`를 1~3줄로 설명한다.
6. 글 마지막은 `정리`와 `참고`로 닫는다.
7. 표는 비교가 필요한 경우에만 쓰고, 나열형 정보는 리스트로 푼다.
8. 비교표는 빠르게 스캔할 수 있도록 `✅`, `⚠️`, `❌`, `🚀` 같은 이모지를 제한적으로 사용할 수 있다.
9. 핵심 용어와 각 용어의 역할은 글 앞부분의 표나 짧은 목록에서 먼저 구분한다.
10. 같은 개념을 여러 절에서 반복하지 않고, 처음 등장하는 관련 절에 설명을 합친다.
11. 기술의 변화 과정을 다룰 때는 `기존 선택의 배경 → 환경 변화 → 새로운 선택의 이유`가 이어지도록 배치한다.
12. 시점에 따라 달라지는 버전, 지원 상태, 로드맵은 기준일과 공식 출처를 함께 남긴다.
13. 이미지는 텍스트보다 관계나 구조를 더 잘 설명할 때만 사용하고, 장식 목적의 이미지는 넣지 않는다.
14. 게시 전에는 Markdown 원본과 Jekyll 빌드 결과에서 이미지 경로를 모두 확인한다.

## 기존 글 참고 범위

- 기술 문서의 형식을 정리할 때는 `_posts/Unity`, `_posts/Programming`, `_posts/Git` 아래의 글을 참고한다.
- 최근에 정리된 기술 글과 이 스타일 가이드를 우선하고, 오래된 글의 불규칙한 제목이나 목록 형식을 그대로 복제하지 않는다.
- Clobie 글과 페이지는 기술 문서 형식의 참고 대상에서 제외한다.
- 기존 글과 통일성을 맞추되, 잘못된 구조까지 유지하지는 않는다.

## 권장 구조

### 개념 정리형

1. 핵심 요약
2. 한눈에 보는 전체 구조 또는 핵심 용어
3. 왜 중요한가
4. 핵심 개념 설명
5. 문제가 생기는 패턴 또는 예시
6. 실무에서 줄이는 방법
7. 정리
8. 참고

### 비교형

1. 핵심 요약
2. 한눈에 비교
3. 항목별 설명
4. 언제 무엇을 쓸지
5. 정리
6. 참고

### 절차형

1. 핵심 요약
2. 준비물 또는 전제 조건
3. 단계별 진행
4. 확인 방법
5. 자주 생기는 예외
6. 정리

### 프로젝트 로그형

1. 이번 작업 목표
2. 작업 전 상태
3. 진행한 일
4. 막혔던 점
5. 결과
6. 다음 작업

## 편집 체크리스트

- 제목과 첫 문단만 읽어도 글의 결론이 보이는가
- 섹션 제목만 훑어도 흐름이 이해되는가
- 코드 예시가 설명보다 먼저 길게 나오지 않는가
- 한 문단이 너무 길면 2~3문단으로 나눌 수 있는가
- 같은 의미의 표현이 반복되지 않는가
- 원인과 결과가 떨어진 절에 배치되어 있지 않은가
- 핵심 용어를 이해하기 전에 긴 역사나 세부 구현이 먼저 나오지 않는가
- 이미지가 있다면 이미지 앞뒤에 맥락 설명이 있는가
- 이미지 경로가 Markdown 미리보기와 Jekyll 결과에서 모두 유효한가
- 버전 및 로드맵 정보에 기준일과 공식 출처가 있는가

## 우선 정리 대상

1. 최근 기술 글
2. 조회 가능성이 높은 비교/최적화 글
3. 검색 유입용 Git / C# / Unity 지식 글
4. 프로젝트 로그

## 그래픽스 연재

- `_posts/Graphics/Knowledge`의 장기 그래픽스 연재는 `GRAPHICS_POST_GUIDE.md`를 추가로 따른다.
- 연재 주제와 작성 순서는 `GRAPHICS_SERIES_PLAN.md`를 유일한 기준으로 삼는다.
- 그래픽스 글은 엔진 독립적인 원리와 역사를 다루며, 엔진별 구현 글은 별도 연재로 분리한다.
- 예약 작성은 모든 앞 글이 사용자 승인, 커밋과 원격 푸시까지 완료된 경우에만 다음 글 하나를 작성한다.
- 검토·수정 중인 초안이 있으면 예약 작업은 해당 글과 다음 글을 모두 수정하지 않고 승인을 기다린다.
